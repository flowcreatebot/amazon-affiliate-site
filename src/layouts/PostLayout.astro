---
import BaseLayout from './BaseLayout.astro';
import Breadcrumbs from '../components/Breadcrumbs.astro';
import AffiliateDisclosure from '../components/AffiliateDisclosure.astro';

interface Props {
  title: string;
  description: string;
  canonical: string;
  ogTitle?: string;
  ogDescription?: string;
  ogType?: 'website' | 'article';
  ogUrl?: string;
  ogImage?: string;
  robots?: string;
  breadcrumbs: Array<{ label: string; href?: string }>;
  showDisclosure?: boolean;
  disclosureText?: string;
}

const {
  breadcrumbs,
  showDisclosure = true,
  disclosureText,
  ...baseProps
} = Astro.props;
---
<BaseLayout {...baseProps}>
  <slot name="head" slot="head" />
  <Breadcrumbs items={breadcrumbs} />
  {showDisclosure && <AffiliateDisclosure text={disclosureText} />}
  <slot />
  {showDisclosure && <AffiliateDisclosure text={disclosureText ?? 'Coffee Gear Lab may earn a commission from qualifying purchases made through links on this page. This does not change the price you pay.'} />}
</BaseLayout>
